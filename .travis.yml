
language: node_js
node_js: stable

branches:
  only:
    - hexo
# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo c
  - hexo g

after_script:
  - git config user.name "ncc0706"
  - git config user.email "ncc0706@gmail.com"
  - sed -i "s/gh_token/${GH_TOKEN}/g" ./_config.yml
  - git d
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
 global:
   - GH_REF: github.com/4520/blog.git




